artifactName: ${productName}-${os}-${arch}.${ext}
win:
  target: nsis
linux:
  target: AppImage
  synopsis: Atlas Desktop
  category: Chat
mac:
  target: dmg
  category: public.app-category.social-networking
files: ['backend/build/**/*', 'frontend/build/**/*']
extraResources: ['./assets/**']
directories:
  buildResources: 'assets'
